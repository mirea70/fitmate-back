bootJar { enabled = false }
jar { enabled = true }

dependencies {
    // 모듈
    api project(':infra-rdbms')
    implementation project(':core')
    // 테스트
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'com.h2database:h2'
}

tasks.named('test') {
    useJUnitPlatform()
}